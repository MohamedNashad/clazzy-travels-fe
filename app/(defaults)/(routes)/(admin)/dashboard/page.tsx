import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
    title: 'CLAZZY TR Dashboard',
};

const Dashboard = () => {
    return <div>starter page</div>;
};

export default Dashboard;
